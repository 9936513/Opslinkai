/** @type {import('next').NextConfig} */
const nextConfig = {
  // App directory is enabled by default in Next.js 13.4+
  // Updated to remove deprecated appDir configuration
}

module.exports = nextConfig
